services:
  - type: web
    name: resume-pdf-server
    env: node
    region: oregon
    plan: free
    buildCommand: "npm install"
    startCommand: "node src/index.js"
    envVars:
      - key: NODE_ENV
        value: production
